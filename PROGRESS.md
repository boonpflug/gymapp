# BUILD PROGRESS

## Last updated: 2026-03-23

## TARGET CUSTOMER
Kieser Training AG (KTAG) — Swiss/German fitness chain, ~2,000 staff, ~250,000 customers, ~44 proprietary machines with biofeedback sensors. Requirements documented in requirements.pdf (Feb 2026). See summary.md for full gap analysis.

## COMPLETED MODULES ✅
- docker-compose.yml — PostgreSQL, Redis, RabbitMQ, backend, frontend all wired (infra/docker-compose.yml)
- Backend project — Spring Boot 3.3.5, Java 21, Maven, all dependencies configured
- Backend Dockerfile — multi-stage Maven build + JRE 21 runtime
- Frontend Dockerfile — Node 20 alpine, Vite dev server
- Liquibase migrations — public schema (tenants, users, refresh_tokens, audit_logs) + tenant schema (users, members, member_notes, member_tags, contracts, membership_tiers, idle_periods, payment_methods, invoices, payments, dunning_levels, dunning_runs, access_devices, check_ins, access_restrictions, access_events, class_categories, classes, class_schedules, class_bookings, waitlist_entries)
- Multi-tenancy — TenantContext, TenantInterceptor (X-Tenant-ID header), TenantConnectionProvider (SET search_path), TenantIdentifierResolver, TenantProvisioningService (creates schema + runs Liquibase), TenantWebMvcConfig
- Tenant module — Tenant entity (public schema), TenantRepository, TenantService, TenantController (SUPER_ADMIN), DTOs
- Auth — JWT (jjwt 0.12.6), refresh token rotation, BCrypt passwords, 6 roles (SUPER_ADMIN, STUDIO_OWNER, MANAGER, TRAINER, MEMBER, RECEPTIONIST), JwtAuthenticationFilter, SecurityConfig, register/login/refresh/logout endpoints
- Shared infrastructure — BaseEntity (UUID PK, auditing), ApiResponse envelope, PageMeta, BusinessException, GlobalExceptionHandler, AuditLog entity + service + repository
- Config — RabbitMQ (3 exchanges + member.checkedin queue), OpenAPI/Swagger, WebSocket (STOMP), CORS, RestTemplate
- Member module — Member entity (with embedded Address), MemberNote, MemberTag, MemberRepository (JpaSpecificationExecutor), MemberService (CRUD, search with specs, notes, RabbitMQ events, audit logging), MemberController (paginated search, role-based access), all DTOs
- Contract module — MembershipTier entity, Contract entity, IdlePeriod entity, ContractStatus enum, BillingCycle enum, MembershipTierService (CRUD), ContractService (create, freeze with billing date adjustment, unfreeze, cancel with notice period enforcement, withdraw cancellation), MembershipTierController, ContractController, all DTOs
- Finance module — PaymentMethod, Invoice, Payment, DunningLevel, DunningRun entities + repos, InvoiceService (generate with VAT, mark paid/overdue), StripePaymentAdapter (real Stripe SDK integration), GoCardlessPaymentAdapter (stub — see IN PROGRESS), PaymentService (dispatches to Stripe/GoCardless/Cash, publishes failure events), PaymentRunService (daily @Scheduled cron job), DunningService (daily escalation with level progression + notification events), StripeWebhookController (signature verification, payment_intent.succeeded/failed), GoCardlessWebhookController (payment confirmed/failed/charged_back handling), InvoiceController, PaymentMethodController, all DTOs
- Frontend scaffold — React 18 + TypeScript + Vite + TanStack Query + Zustand + Tailwind CSS, API client with JWT interceptor + tenant header, auth store with persist + refresh, login page, dashboard page, members page (paginated table with search), contracts page (tier cards), Layout with sidebar nav
- Check-in module — CheckIn, AccessDevice, AccessRestriction, AccessEvent entities + repos, CheckInMethod/CheckInStatus/DeviceType/DeviceMode/RestrictionReason/AccessEventType enums, AccessControlAdapter interface + HttpRestAccessControlAdapter (HTTP REST for doors/gates/turnstiles) + MqttAccessControlAdapter (MQTT for IoT devices), CheckInService (manual + device check-in, access validation: member status, active restrictions, active contract, double check-in prevention, occupancy limit enforcement, check-out), AccessRestrictionService (create/release/auto-release restrictions), OccupancyService (real-time count + WebSocket broadcast), AccessDeviceService (CRUD, toggle, device status via adapter), AccessEventService (event log queries), CheckInController (manual check-in, device check-in, check-out, history, recent, current, occupancy endpoints, role-based @PreAuthorize), AccessDeviceController (CRUD, status), AccessRestrictionController (member restrictions, create, release), AccessEventController (live event log, filter by device/member), all DTOs, Liquibase migration t-005-checkin with indexes, RabbitMQ member.checkedin event publishing, WebSocket occupancy broadcast (/topic/occupancy/{tenantId}) + access event broadcast (/topic/access-events/{tenantId})
- Frontend check-in page — member search with one-click manual check-in, live occupancy badge (WebSocket + polling), recent check-ins table with check-out action, live event monitor sidebar (STOMP WebSocket), @stomp/stompjs + sockjs-client deps, CheckIn/Occupancy/AccessDevice/AccessEvent/AccessRestriction TypeScript types
- Class booking module — ClassCategory, ClassDefinition, ClassSchedule, ClassBooking, WaitlistEntry entities + repos, BookingStatus/WaitlistStatus/RecurrenceRule enums, ClassCategoryService (CRUD + deactivate), ClassDefinitionService (CRUD with category validation), ClassScheduleService (weekly schedule, date range queries, create with recurrence rule generation, cancel schedule with cascading booking cancellation + waitlist expiry, trainer schedule), ClassBookingService (book with cutoff enforcement + capacity check + duplicate prevention + auto-waitlist when full, cancel with cutoff + auto-promote from waitlist, mark attendance ATTENDED/NO_SHOW, member booking history, schedule bookings/waitlist queries, cancel waitlist entry, RabbitMQ booking.created events, audit logging), ClassCategoryController, ClassDefinitionController, ClassScheduleController, ClassBookingController (all with @PreAuthorize role-based access), all DTOs (ClassCategoryDto, ClassDefinitionDto, ClassScheduleDto, ClassBookingDto, WaitlistEntryDto, CreateClassCategoryRequest, CreateClassRequest, CreateScheduleRequest, CreateBookingRequest, MarkAttendanceRequest), Liquibase migration t-006-booking with FKs and indexes
- Frontend classes page — tabbed UI (Weekly Schedule / Class Definitions / Categories), weekly schedule grid with day columns + clickable class cards showing booked/capacity + waitlist count, schedule detail panel (bookings list with attendance marking Present/No Show, waitlist display, cancel class, virtual meeting link), class definitions table, categories card grid with deactivate, create category modal (name, description, color picker), create class modal (full form: name, description, category, capacity, duration, room, virtual link, cutoffs, waitlist/trial toggles), create schedule modal (class selector, date/time, recurrence rule with occurrence count), ClassCategory/ClassDefinition/ClassSchedule/ClassBooking/WaitlistEntry TypeScript types
- Training module — Exercise entity (name, description, type, muscle groups, equipment, video URL, thumbnail, execution tips, posture notes, difficulty, global/tenant-scoped), TrainingPlan entity (name, description, member, trainer, status DRAFT/PUBLISHED/ARCHIVED, template/catalog flags, category, duration, difficulty), TrainingPlanExercise entity (plan-exercise link with sort order, sets, reps, weight, rest, trainer comment, superset group), TrainingSession entity (member, plan, start/finish time, duration, notes, rating), TrainingLog entity (session-exercise log with set number, target/actual reps/weight, duration, completed flag), TrainingGoal entity (member goal with type, title, target/current value, unit, dates, status ACTIVE/ACHIEVED/ABANDONED), ExerciseType enum (12 types), MuscleGroup enum (20 groups), GoalType/GoalStatus enums, ExerciseSpecification (search by name/muscle/type/equipment), ExerciseService (CRUD, search with specs, deactivate, equipment list), TrainingPlanService (create with exercises, update, publish with validation, archive, create from template with deep copy, reorder exercises, member plans, templates, catalog), TrainingSessionService (start/finish session with duration calc, log exercises with validation, member session history, exercise progress history, session count), TrainingGoalService (create, update progress with auto-achieve, abandon, member goals), ExerciseController, TrainingPlanController, TrainingSessionController, TrainingGoalController (all with @PreAuthorize role-based access), all DTOs (ExerciseDto, TrainingPlanDto, TrainingPlanExerciseDto, TrainingSessionDto, TrainingLogDto, TrainingGoalDto, CreateExerciseRequest, CreateTrainingPlanRequest, PlanExerciseRequest, StartSessionRequest, FinishSessionRequest, LogExerciseRequest, CreateGoalRequest, UpdateGoalProgressRequest, ReorderExercisesRequest), Liquibase migration t-007-training with all 6 tables + FKs + indexes, RabbitMQ event publishing (training.plan.published, training.session.completed), audit logging on all mutations
- Frontend training page — tabbed UI (Exercise Library / Training Plans / Templates / Catalog / Goals), exercise library with search + muscle group + exercise type filters + card grid with detail modal, create exercise modal (full form), training plan list with member ID filter + create plan modal (exercise search + add + drag-to-reorder + sets/reps/weight/rest/comment per exercise + template/catalog toggles), plan detail modal with exercise list + publish/archive actions, template list view, catalog list view, goals list with progress bars + inline progress update + abandon, create goal modal, ExerciseDto/TrainingPlanDto/TrainingSessionDto/TrainingLogDto/TrainingGoalDto TypeScript types + all enums
- Communication module — CommunicationTemplate entity (name, channel type EMAIL/SMS/PUSH/LETTER/WHATSAPP, subject, body HTML/text, category, locale, logo URL, brand color), NotificationRule entity (name, trigger event with 14 types: BIRTHDAY/NEW_MEMBERSHIP/PAYMENT_FAILED/PAYMENT_SUCCESS/APPOINTMENT_REMINDER/CONTRACT_EXPIRY/CONTRACT_CANCELLATION/TRIAL_BOOKED/MEMBERSHIP_ANNIVERSARY/CHECKIN_MILESTONE/CLASS_REMINDER/TRAINING_PLAN_PUBLISHED/WELCOME/CUSTOM, template link, channel, delay days/direction BEFORE/AFTER/IMMEDIATE), SentMessage entity (member, template, channel, subject, body preview, recipient address, status PENDING/SENT/DELIVERED/FAILED/OPENED, sent/delivered/failed/opened timestamps, error message, trigger event, sent by), CommunicationTemplateService (CRUD, deactivate, variable interpolation with {{member.firstName}} pattern, preview), NotificationRuleService (CRUD, toggle active, delete, template validation), MessageService (send with template interpolation + channel dispatch EMAIL/SMS/PUSH/WHATSAPP/LETTER, send from rule for automated notifications, resend failed, member history, status filter, message stats with counts by status), NotificationEventConsumer (RabbitMQ @RabbitListener on notification.events.queue + member.events.notification.queue, maps events to triggers, finds matching active rules, dispatches immediate messages, logs delayed rules for scheduled processing), RabbitMQ config updated with notification.events.queue + member.events.notification.queue + payment.events.notification.queue bindings, CommunicationTemplateController, NotificationRuleController, MessageController (all with @PreAuthorize STUDIO_OWNER/MANAGER), all DTOs, Liquibase migration t-008-communication with 3 tables + 3 FKs + 6 indexes, audit logging on all mutations
- Frontend communication page — tabbed UI (Templates / Notification Rules / Message History / Stats), template list with channel filter + create modal (name, channel, subject, body HTML with variable placeholder hint, body text, category, locale, brand color picker) + detail modal with deactivate, notification rules list with trigger/channel/delay display + active toggle + delete + create modal (trigger event selector, template picker, channel, delay direction/days), message history table with status filter + resend failed + status badges, stats dashboard (total sent, delivered, opened, failed, pending cards + delivery rate progress bar), CommunicationTemplateDto/NotificationRuleDto/SentMessageDto/MessageStatsDto TypeScript types + ChannelType/MessageStatus/TriggerEvent/DelayDirection enums
- Sales module — Lead entity (first/last name, email, phone, source with 10 sources WEBSITE/WALK_IN/REFERRAL/SOCIAL_MEDIA/GOOGLE_ADS/META_ADS/PARTNER/PHONE/EVENT/OTHER, interest, stage link, assigned staff, notes, converted member link, referral member link), LeadStage entity (name, sort order, color, isDefault/isClosed/isWon flags), LeadActivity entity (lead link, activity type with 10 types CALL/EMAIL/VISIT/NOTE/TASK/SMS/MEETING/TRIAL_BOOKED/PROPOSAL_SENT/CONTRACT_SIGNED, description, outcome, staff, due date, completed timestamp), PromoCode entity (code unique, description, discount type PERCENTAGE/FIXED, discount value, expiry, max/current usages, active), PromoCodeUsage entity (promo code + member + lead + contract links, used timestamp), LeadSpecification (search by name/source/stage/assigned staff), LeadStageService (CRUD, delete with lead count check, init 6 default stages: New/Contacted/Trial Booked/Proposal Sent/Converted/Lost), LeadService (CRUD, search with specs, move to stage with activity log, assign staff, convert to member with auto-create Member + move to won stage + RabbitMQ member.created event, pipeline summary with conversion rate), LeadActivityService (CRUD, complete task with outcome, overdue tasks query, pending tasks by staff), PromoCodeService (CRUD, validate with expiry/usage checks, redeem with atomic increment + usage record, deactivate), LeadStageController, LeadController (with pipeline endpoint), LeadActivityController (with overdue/my-tasks endpoints), PromoCodeController (with validate endpoint), all DTOs (LeadStageDto, LeadDto, LeadActivityDto, PromoCodeDto, SalesPipelineDto, CreateLeadStageRequest, CreateLeadRequest, CreateLeadActivityRequest, CreatePromoCodeRequest, MoveLeadRequest, ConvertLeadRequest), Liquibase migration t-009-sales with 5 tables + 3 FKs + 7 indexes, audit logging on all mutations
- Frontend sales page — tabbed UI (Pipeline / All Leads / Promo Codes), pipeline view with Kanban-style columns (one per stage, color-coded, lead cards with name/source/interest/email/assigned staff/activity count), conversion stats cards (total leads, converted, conversion rate %), initialize default stages button, all leads table with name search + source filter + convert action + create lead modal (name, email, phone, source, interest, notes), lead detail modal with activity log + add activity form (type selector, description, outcome, due date for tasks), promo codes card grid with code/discount/usage/expiry/status display + create promo modal (code, description, discount type/value, expiry datetime, max usages) + deactivate action, LeadDto/LeadStageDto/LeadActivityDto/PromoCodeDto/SalesPipelineDto TypeScript types + LeadSource/LeadActivityType/DiscountType enums

- Staff module — Employee entity (user link, first/last name, email, phone, role, employment type FULL_TIME/PART_TIME/FREELANCE/INTERN, position, hourly rate, monthly salary, hire/termination dates, competencies, active flag), EmployeeFacility entity (employee-facility many-to-many with primary flag for multi-location staff), Shift entity (employee, facility, start/end time, status SCHEDULED/CONFIRMED/IN_PROGRESS/COMPLETED/CANCELLED/NO_SHOW, notes), TimeEntry entity (employee, shift link, clock in/out timestamps, break minutes, total minutes auto-calculated, notes), EmployeeService (CRUD with facility assignments, deactivate), ShiftService (create with time validation, update status, cancel, employee schedule queries, weekly schedule, upcoming shifts, shift vs actual hours report with grouped comparison per employee), TimeTrackingService (clock in with duplicate prevention + auto-set shift IN_PROGRESS, clock out with duration calc + break deduction + auto-complete shift, active entry lookup, employee time entries by date range, total minutes aggregation), EmployeeController, ShiftController (with weekly/report endpoints), TimeTrackingController (clock-in/out/active/total endpoints), all DTOs (EmployeeDto, ShiftDto, TimeEntryDto, ShiftReportDto, CreateEmployeeRequest, CreateShiftRequest, ClockInRequest, ClockOutRequest), Liquibase migration t-010-staff with 4 tables + 4 FKs + 6 indexes, audit logging on all mutations
- Frontend staff page — tabbed UI (Employees / Shift Schedule / Time Tracking), employee table with name/role/type/position/contact + create modal (name, email, phone, employment type, role, position, hourly rate, competencies) + deactivate action, shift schedule list with employee name + datetime + duration + status badges + cancel action + create modal (employee picker, start/end datetime, notes), time tracking panel with employee selector + live clock-in status indicator (green pulse) + clock-in button + clock-out with optional break deduction + current session duration display, EmployeeDto/ShiftDto/TimeEntryDto/ShiftReportDto TypeScript types + EmploymentType/ShiftStatus enums

- Multi-location module — Facility entity (name, description, address fields, timezone, phone, email, website, opening hours JSON, logo URL, brand color, banner image, max occupancy, parent facility for franchise, active flag, tenant_id), FacilityConfiguration entity (facility_id, config_key, config_value, description — key-value per-facility settings), MemberFacilityAccess entity (member_id, facility_id, home_facility flag, cross_facility_access flag — cross-facility member access rights), FacilityRepository, FacilityConfigurationRepository, MemberFacilityAccessRepository, FacilityService (CRUD with parent validation, deactivate, per-facility configuration get/set/delete, member facility access: assign with home facility toggle + duplicate prevention, remove, access check for cross-facility check-in), ConsolidatedDashboardService (JdbcTemplate queries: total active members, check-ins today, new members this month, revenue this month, outstanding payments, per-facility breakdown with occupancy/revenue/check-ins/members), FacilityController (paginated list, detail with child facilities, create/update/deactivate, config CRUD, member access CRUD, can-access check, consolidated dashboard endpoint, @PreAuthorize STUDIO_OWNER for mutations + MANAGER for reads), all DTOs (FacilityDto, CreateFacilityRequest, FacilityConfigDto, SetFacilityConfigRequest, MemberFacilityAccessDto, AssignMemberFacilityRequest, ConsolidatedDashboardDto with FacilitySummaryDto), Liquibase migration t-011-facility with 3 tables (facilities, facility_configurations, member_facility_access) + 5 FKs (parent facility self-ref, config→facility, member_access→member, member_access→facility, employee_facilities→facility) + 7 indexes (parent, active, config facility_id, config unique key, member access member_id, member access facility_id, member access unique pair), audit logging on all mutations
- Frontend facilities page — tabbed UI (Consolidated Dashboard / Facilities), consolidated dashboard with 6 stat cards (total facilities, active members, check-ins today, new this month, revenue, outstanding) + per-facility breakdown cards (name, city, brand color, members, check-ins, new members, occupancy with capacity, revenue), facility management with card grid (name, description, address, phone, email, timezone, max occupancy, parent facility, member count, brand color dot, active badge, edit/deactivate actions), create/edit modal (full form: name, description, street, city, state, postal code, country, timezone, phone, email, website, max occupancy, brand color picker, parent facility franchise selector, opening hours), FacilityDto/FacilityConfigDto/MemberFacilityAccessDto/ConsolidatedDashboardDto/FacilitySummaryDto TypeScript types

- Member self-service portal — MemberPortalController (@PreAuthorize MEMBER role, resolves member from JWT userId via MemberRepository.findByUserId), endpoints: GET /api/portal/profile (view own profile), PUT /api/portal/profile (update own profile via UpdateMemberRequest), GET /api/portal/contracts (own contracts), POST /api/portal/contracts/{id}/cancel (cancel with ownership check + freeze suggestion), POST /api/portal/contracts/{id}/withdraw-cancellation (withdraw with ownership check), GET /api/portal/invoices (own invoices), GET /api/portal/invoices/{id} (single invoice with ownership check), GET /api/portal/checkins (own check-in history, paginated), GET /api/portal/occupancy (live studio occupancy), all endpoints enforce member can only access their own data
- Frontend member portal — separate /portal route with dedicated PortalLayout (emerald-themed sidebar, member name, 7 nav items), PortalDashboard (membership status card, active contracts card with next billing, live occupancy card with 30s auto-refresh, contracts overview list with status badges), PortalProfile (read-only display + edit mode toggle, full form: name, email, phone, DOB, gender, address fields, emergency contact, save/cancel), PortalContracts (contract cards with tier name, dates, amount, status badges, cancel flow with freeze-suggestion popup before proceeding, cancellation reason form, withdraw cancellation button for pending contracts), PortalInvoices (outstanding invoices highlight section, full invoice table: number, issued date, due date, amount, total, status badges, pagination), PortalClasses (tabbed: Weekly Schedule with 7-day grid + book/join-waitlist buttons + booked indicator, My Bookings list with cancel action), PortalTraining (tabbed: My Plans with exercise list expand, Sessions history with duration/rating/status, Goals with progress bars), PortalCheckins (paginated table: date/time, method, status badge, check-out time), MemberDto/ContractDto/InvoiceDto TypeScript types updated with full backend fields

## IN PROGRESS 🔄
- GoCardless adapter — stub implementation only, needs real GoCardless Java SDK integration
  - Working: payment creation (returns mock ID), webhook handling (confirmed/failed/charged_back)
  - NOT DONE: real API calls via gocardless-pro SDK, mandate creation flow
  - Next: add `com.gocardless:gocardless-pro` dependency to pom.xml and implement real API calls in GoCardlessPaymentAdapter.java

- Public studio discovery portal — PublicPortalController at /api/public/{tenantSlug}/* (no auth required, open in SecurityConfig), endpoints: GET /{tenantSlug}/profile (studio profile from primary facility: name, description, address, phone, email, hours, branding, all facility locations), GET /{tenantSlug}/classes/categories (active class categories with colors), GET /{tenantSlug}/classes/schedule (weekly class schedule via ClassScheduleService), POST /{tenantSlug}/booking/trial (guest trial booking: creates lead from booking if new email + books class as guest via ClassBookingService), POST /{tenantSlug}/contact (lead capture form: creates Lead with WEBSITE source in default stage), tenant resolution via schema name lookup, DTOs (StudioProfileDto with FacilitySummary, PublicTrialBookingRequest, PublicContactRequest), added findByEmail to LeadRepository, added findByActiveTrueOrderByNameAsc to ClassCategoryRepository
- Frontend public studio page — /studio/:slug route (no login required), branded header with studio name/description/address using facility brandColor, sticky nav with 4 tabs (About / Class Schedule / Book Trial / Contact), About tab (studio description, contact info, opening hours parsed from JSON, multi-location list), Class Schedule tab (7-day weekly grid with category color dots, booked/capacity counts), Book Trial tab (class selector with radio buttons, guest form: first/last name, email, phone, booking confirmation success state), Contact tab (lead capture form: name, email, phone, interest dropdown, message, success state), footer with studio branding, all mutations with loading/error states
- DataSeeder (step 26 partial) — ApplicationRunner that seeds demo data on first startup when demo-gym tenant doesn't exist: creates tenant in public schema, provisions tenant_demo_gym schema via Liquibase, seeds 55 users (owner/manager/2 trainers/receptionist/50 members), 3 facilities (Berlin Downtown/West + Munich franchise), 4 membership tiers, 50 members with mixed statuses, 50 contracts, 3 access devices, 415 check-ins over 30 days, 5 class categories + 10 classes + 32 schedules, 40 exercises + 13 training plans, 6 lead stages + 20 leads, 6 communication templates + 3 notification rules, 5 employees, 90 invoices
- Bug fixes: removed hibernate.default_schema=public from application.yml (was causing Hibernate to qualify all tables with public. prefix, breaking multi-tenant schema routing via SET search_path), removed /api/auth/** from TenantInterceptor exclusions (auth endpoints need tenant context for login to find users in tenant schema), fixed TenantProvisioningService subdomain hyphen handling in DataSeeder

- Marketing module — Campaign entity (name, description, type EMAIL/SMS/PUSH, status DRAFT/SCHEDULED/SENDING/SENT/CANCELLED, template link, subject, body HTML/text, audience criteria JSON, scheduled/sent timestamps, recipient/sent/delivered/opened/clicked/failed/converted counts, created by), CampaignRecipient entity (campaign link, member link, recipient address, status per-recipient SENT/DELIVERED/OPENED/CLICKED/FAILED, sent/delivered/opened/clicked timestamps, error message), CampaignEvent entity (campaign link, recipient link, member link, event type, metadata), CampaignStatus/CampaignType/CampaignEventType enums, CampaignRepository (JpaSpecificationExecutor, find by status, find scheduled due), CampaignRecipientRepository (by campaign, count by status grouped), CampaignEventRepository (by campaign + type), AudienceBuilderService (build audience from AudienceCriteria: member status filter, gender, join date range, age range via JPA Specification + check-in inactivity filter + check-in frequency filter + contract status filter + contract expiry filter, preview with sample members), AtRiskDetectionService (detect at-risk active members: no check-in analysis, visit frequency trend 30-day vs 60-day comparison, risk level HIGH/MEDIUM/LOW classification, summary by risk level), CampaignService (create/update draft campaigns with audience criteria JSON serialization, schedule with audience resolution + recipient creation, send now with template interpolation + RabbitMQ notification.events dispatch per recipient + campaign event logging, cancel, get recipients paginated, overall stats aggregation, @Scheduled processScheduledCampaigns every 60s for auto-send), CampaignController (@PreAuthorize STUDIO_OWNER/MANAGER, CRUD + schedule/send/cancel + recipients + stats + audience preview endpoints), AtRiskController (at-risk members list with configurable inactive days threshold + summary by risk level), all DTOs (CampaignDto with computed delivery/open/click rates, CampaignRecipientDto with member name/email denormalized, CampaignStatsDto, AtRiskMemberDto with visit trend/risk analysis, AudienceCriteria, AudiencePreviewDto with sample members), Liquibase migration t-012-marketing with 3 tables (campaigns, campaign_recipients, campaign_events) + 7 indexes, RabbitMQ campaign.message.queue bound to notification.events exchange, audit logging on all mutations
- Frontend marketing page — tabbed UI (Campaigns / Audience Builder / At-Risk Members), campaigns tab with stats cards (total campaigns, sent, delivered, opened, avg open rate) + status filter dropdown + campaigns table (name, type badge, status badge, recipients, sent count, open rate, send/schedule/cancel actions) + pagination + create campaign modal (name, type, description, subject, body HTML with variable placeholder hint, schedule datetime, audience criteria: member status multi-select, no-check-in days, contract status, contract expiry days, gender + inline audience preview with matching count + sample members) + campaign detail modal (performance stats grid + delivery/open/click rate progress bars + recipients table with status badges + pagination), audience builder tab (split panel: criteria form with member status, inactivity days, check-in frequency range, contract status, contract expiry days, join date range, gender, age range + preview button; results panel with total count highlight + sample members table), at-risk members tab (risk summary cards HIGH/MEDIUM/LOW with color coding + configurable inactive threshold input + members table with name/email, risk level badge, last visit date, days inactive, avg weekly visits, visit trend % with color, risk reason), CampaignDto/CampaignRecipientDto/CampaignStatsDto/AudienceCriteria/AudiencePreviewDto/AtRiskMemberDto TypeScript types + CampaignStatus/CampaignType/CampaignEventType/RiskLevel enums

## COMPLETED (this session) ✅
- Loyalty & rewards module (step 22) — points system (configurable per action), 4 tiers (Bronze/Silver/Gold/Platinum), rewards catalog (5 rewards), badge system (8 badges with auto-award), streak tracking (daily/weekly), referral program (unique codes, point awards), staff dashboard, seed data (431 transactions across 20 members), frontend 5-tab page
- Appointment & agenda module (step 23) — appointment types, staff availability (recurring + one-off), full lifecycle (schedule/confirm/start/complete/cancel/no-show), overlap detection, anamnese form builder (7 question types), submission system, staff day agenda view, frontend 4-tab page
- Kieser machine sensor integration (step 24) — machine registry with inventory management, maintenance logging, sensor session recording linked to training sessions, strength measurements (isometric/dynamic/ROM), member progress tracking with improvement %, machine utilization analytics, frontend 4-tab page

## NOT STARTED ⏳

### Core Platform (original roadmap)
- Open API + webhook system (step 29)
- Analytics & reporting dashboards with CSV/PDF export (step 30)
- Mobile app — React Native + Expo (step 25)
- Staff & trainer floor app — React Native role-aware view (step 19)
  NOTE: Steps 19 and 25 (mobile apps) require React Native + Expo setup — skip if focusing on web-only modules first
- Seed data script (step 26) — PARTIAL: DataSeeder exists, full seed spec (500 exercises, sample payment history) not yet complete
- Platform SaaS onboarding — self-service signup, trial, Stripe Billing (step 4/6)
- Super admin dashboard — /superadmin tenant management, feature flags (step 4)

### KTAG-Specific Requirements (from requirements.pdf, Feb 2026)

**Must-Have (launch blockers for Kieser):**
- Kieser machine sensor integration (LE/CE) — biofeedback data ingestion from proprietary machines, strength measurement (Kraftmessung), link sensor data to training sessions/logs
- Swiss payment methods — QR-Rechnung (ISO 20022 QR-bill) + LSV/LSV+ (Swiss direct debit), replacing/supplementing GoCardless SEPA
- Internationalization (i18n) — UI + correspondence in DE, FR, EN; locale-aware date/currency/number formatting for DACH + Luxembourg
- Azure SSO — SAML/OIDC enterprise authentication alongside existing JWT auth
- Contract auto-renewal — automatic renewal logic with configurable rules (currently missing from contract lifecycle)
- DATEV export — German accounting standard export format for finance module
- Appointment system — 1-on-1 booking (personal training, assessments, physio) separate from group class scheduling; day plan / agenda view for staff

**Should-Have (important for KTAG operations):**
- Gantner access control adapter — concrete AccessControlAdapter implementation for Gantner hardware (doors/turnstiles)
- seca BIA scale integration — body composition measurement data import, link results to member profile
- Machine inventory management — equipment/machine registry per facility, maintenance tracking, Kieser's ~44 proprietary machine types
- Anamnese form builder — structured health assessment forms, store results per member, link to training plans
- Physical mail (Post) — PDF letter generation from templates, postal dispatch integration (Serienbriefe)
- Credit notes (Gutschriften) — refund document generation, credit balance management
- Split invoices — invoice splitting across multiple payers/cost centers
- TSE integration — German fiscal security device for cash register compliance
- E-Rechnung — ZUGFeRD/XRechnung structured electronic invoice format
- Legacy data migration tooling — import from KTAG's existing system (Bestandsystem)

**Nice-to-Have (phased rollout):**
- EBICS banking interface — direct bank communication for automated payment processing
- ERP integration — connector/API for enterprise resource planning systems
- GOAv billing — German medical fee schedule (Gebührenordnung für Ärzte) for physio billing
- Revenue recognition (Abgrenzungsmodelle) — deferred revenue / accrual accounting models
- Cooperation management (Kooperationsmanagement) — partner relationships, co-marketing
- Service-Center integration — call center / support desk connectivity
- Tarif 595 — Swiss health insurance billing for physiotherapy (scope TBD per KTAG)
- IDW PS 880 — German software audit standard compliance documentation
- NIS2 — EU cybersecurity directive compliance
- Accessibility (Barrierefreiheit) — WCAG 2.1 AA compliance
- Global product/contract catalog — cross-tenant shared catalog for franchise standardization
- Franchise fee billing — automated franchise fee calculation and invoicing
- Voucher sales — purchasable gift vouchers (online + POS), recipient redemption at signup

## KEY DECISIONS MADE
- Tenant resolved from X-Tenant-ID header (not subdomain)
- UUIDs used for all PKs — do not change to Long
- DTOs in separate /dto package per module
- RabbitMQ exchanges: member.events, payment.events, notification.events
- S3 bucket structure: /{tenantId}/contracts/, /{tenantId}/media/
- Schema-per-tenant via Hibernate MultiTenantConnectionProvider + SET search_path
- Liquibase: public schema changelog (db.changelog-master.yaml) + tenant schema changelog (db.changelog-tenant.yaml) run by TenantProvisioningService
- JWT claims include: sub (userId), email, role, tenantId, firstName
- No MapStruct used yet — manual toDto() methods in services for simplicity
- Frontend uses Vite proxy to backend at localhost:8080

## CURRENT BUGS / KNOWN ISSUES
- GoCardless sandbox returning 422 on mandate creation — investigating (adapter is stub only)
- Exercise video streaming not chunked yet, fix before demo
- IDE shows Lombok errors (getters/setters "undefined") — this is annotation processor not running in IDE, Maven compile succeeds

## DO NOT CHANGE
- Database schema for members, contracts tables — already in production seed
- JWT secret key format — changing breaks existing tokens
- RabbitMQ exchange names — other consumers depend on these
- Multi-tenancy approach (schema-per-tenant with TenantContext ThreadLocal)
- BaseEntity UUID strategy (GenerationType.UUID)

## FILE COUNTS
- Backend Java files: 321
- Frontend TypeScript/TSX files: 28
- Config/migration YAML files: 5
- Total source files: ~354

---

### Resume prompt — paste this at the start of every new session
```
Read PROGRESS.md and MASTER_PROMPT.md in this repo first.

PROGRESS.md tells you exactly what's done and what's in progress.
MASTER_PROMPT.md is the full spec for the entire platform.

Do not re-build anything marked ✅ in PROGRESS.md.
Do not change any decisions listed under KEY DECISIONS MADE.
Do not alter the database schema for completed modules.

Resume from: Next: implement Kieser machine sensor integration (step 24) — LE/CE biofeedback, strength measurement, machine inventory. Then: i18n (step 25), Swiss payments (step 26), enterprise auth (step 27), contract auto-renewal (step 28).
```
